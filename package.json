{
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.9",
    "chokidar-cli": "^3.0.0",
    "concurrently": "^8.0.1",
    "parcel": "^2.8.3",
    "process": "^0.11.10",
    "tailwindcss": "^3.3.1"
  },
  "scripts": {
    "client:clean": "rm -rf build dist .parcel-cache",
    "client:watch": "gleam build && concurrently --kill-others \"chokidar 'client/src/**' -c 'gleam build'\" \"parcel client/src/client.ts --no-hmr --dist-dir priv/static\"",
    "client:build": "npm run client:clean && cd client && gleam build && cd .. && parcel build client/src/client.ts --dist-dir priv/static --no-source-maps",
    "tailwind:build": "npx tailwindcss -i ./app.css -o priv/static/app.css",
    "tailwind:watch": "npx tailwindcss -i ./app.css -o priv/static/app.css --watch",
    "watch": "gleam build && concurrently --kill-others \"gleam run\" \"npm run client:watch\" \"npm run tailwind:watch\""
  },
  "alias": {
    "app": "./client/build/dev/javascript/client/main.mjs",
    "ffi": "./client/src/ffi/"
  },
  "dependencies": {
    "morphdom": "^2.7.0"
  }
}